project_id = "xculwvekfctuqdogdaec"

# Ambiente local de testes: cadastro por e-mail sem etapa de confirmação.
# No projeto hospedado, a mesma opção precisa ser alterada no painel do Supabase.
[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = false
secure_password_change = false
max_frequency = "1m0s"
otp_length = 6
otp_expiry = 3600
